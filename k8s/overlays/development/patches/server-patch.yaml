apiVersion: apps/v1
kind: Deployment
metadata:
  name: ziplink-server
  namespace: ziplink
spec:
  template:
    spec:
      containers:
      - name: ziplink-server
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Development"