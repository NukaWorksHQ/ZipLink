apiVersion: apps/v1
kind: Deployment
metadata:
  name: ziplink-postgres
  namespace: ziplink
spec:
  template:
    spec:
      containers:
      - name: postgres
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "4Gi"
            cpu: "2000m"